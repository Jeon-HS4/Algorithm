# https://school.programmers.co.kr/learn/courses/30/lessons/120850

def solution(my_string):
    answer = []
    my_str = list(my_string)
    for i in range(len(my_str)):
        if(ord(my_str[i]) < 58):
            answer.append(ord(my_str[i])-48)
    answer.sort()
        
    return answer
